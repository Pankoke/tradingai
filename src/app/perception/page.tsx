import type { JSX } from "react";

export default function PerceptionPage(): JSX.Element {
  return (
    <main className="min-h-screen bg-[var(--bg-main)] px-6 py-12">
      <div className="mx-auto max-w-3xl space-y-4 text-[var(--text-primary)]">
        <h1 className="text-2xl font-semibold">Perception Lab</h1>
        <p className="text-sm text-[var(--text-secondary)]">
          TODO: implementieren
        </p>
      </div>
    </main>
  );
}
